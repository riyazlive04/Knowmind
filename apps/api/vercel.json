{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "cd ../.. && npm install",
  "buildCommand": "cd ../.. && npm run build:shared && npm run build:db",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
